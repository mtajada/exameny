{
  "name": "@exameny/luna-evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0 <25"
  },
  "scripts": {
    "dry-run": "node run.mjs --mode dry-run",
    "lint": "node scripts/lint.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run lint && npm test && npm run dry-run"
  }
}
